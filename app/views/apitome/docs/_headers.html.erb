<% unless headers.blank? %>
  <section class="headers">
    <h4><%= t('apitome.headers') %></h4>
    <pre><code class="http"><%= headers.map{ |k,v| "#{k}: #{v}" }.join("\n") %></code></pre>
  </section>
<% end %>
